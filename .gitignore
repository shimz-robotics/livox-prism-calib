__pycache__/
*.py[cod]
*.bak
.DS_Store

# Cursor rules/config — keep local to this machine
.cursor/

# Point cloud CSVs are large; keep locally, do not commit
data/input_data/hap*.csv

# ROS 2 workspace (built locally, see issue #3/#5)
/ros2_livox_ws/
